# =====================================================
# ======================= L1 RAW ======================
# =====================================================

# -----------------------------------------------------
# MACRO - DAILY
# -----------------------------------------------------
macro_daily_raw:
  type: kedro_datasets.partitions.PartitionedDataset
  path: data/01_raw/macro/daily
  filename_suffix: ".parquet"
  dataset:
    type: kedro_datasets.pandas.ParquetDataset
    save_args:
      compression: snappy

# -----------------------------------------------------
# MACRO - WEEKLY
# -----------------------------------------------------
macro_weekly_raw:
  type: kedro_datasets.partitions.PartitionedDataset
  path: data/01_raw/macro/weekly
  filename_suffix: ".parquet"
  dataset:
    type: kedro_datasets.pandas.ParquetDataset
    save_args:
      compression: snappy

# -----------------------------------------------------
# MACRO - MONTHLY
# -----------------------------------------------------
macro_monthly_raw:
  type: kedro_datasets.partitions.PartitionedDataset
  path: data/01_raw/macro/monthly
  filename_suffix: ".parquet"
  dataset:
    type: kedro_datasets.pandas.ParquetDataset
    save_args:
      compression: snappy


# -----------------------------------------------------
# SPOT ASSETS (negociáveis)
# Inclui:
# - Crypto (Binance)
# - Equity indices (S&P, Nasdaq)
# - Commodities (Gold)
# - FX spot
# -----------------------------------------------------
spot_daily_bd_raw:
  type: kedro_datasets.partitions.PartitionedDataset
  path: data/01_raw/spot/business_day
  filename_suffix: ".parquet"
  dataset:
    type: kedro_datasets.pandas.ParquetDataset
    save_args:
      compression: snappy

# -----------------------------------------------------
# SPOT CRYPTO 24/7 (Binance daily)
# -----------------------------------------------------
spot_crypto_daily_24x7_raw:
  type: kedro_datasets.partitions.PartitionedDataset
  path: data/01_raw/spot/crypto/daily_24x7
  filename_suffix: ".parquet"
  dataset:
    type: kedro_datasets.pandas.ParquetDataset
    save_args:
      compression: snappy


# -----------------------------------------------------
# DERIVATIVES (futuro)
# -----------------------------------------------------
derivatives_daily_raw:
  type: kedro_datasets.partitions.PartitionedDataset
  path: data/01_raw/derivatives/daily
  filename_suffix: ".parquet"
  dataset:
    type: kedro_datasets.pandas.ParquetDataset
    save_args:
      compression: snappy


# -----------------------------------------------------
# LIQUIDITY (on-chain / balance sheet / reserves)
# -----------------------------------------------------
liquidity_raw:
  type: kedro_datasets.partitions.PartitionedDataset
  path: data/01_raw/liquidity/daily
  filename_suffix: ".parquet"
  dataset:
    type: kedro_datasets.pandas.ParquetDataset
    save_args:
      compression: snappy


# -----------------------------------------------------
# POSITIONING (COT, funding rates, OI)
# -----------------------------------------------------
positioning_raw:
  type: kedro_datasets.partitions.PartitionedDataset
  path: data/01_raw/positioning/daily
  filename_suffix: ".parquet"
  dataset:
    type: kedro_datasets.pandas.ParquetDataset
    save_args:
      compression: snappy


# -----------------------------------------------------
# SENTIMENT (futuro)
# -----------------------------------------------------
sentiment_raw:
  type: kedro_datasets.partitions.PartitionedDataset
  path: data/01_raw/sentiment/daily
  filename_suffix: ".parquet"
  dataset:
    type: kedro_datasets.pandas.ParquetDataset
    save_args:
      compression: snappy
